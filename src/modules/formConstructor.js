class Form {
  constructor(id, code, formTitle, formShortTitle, parent) {
    this.id = id;
    this.code = code;
    this.formTitle = formTitle;
    this.formShortTitle = formShortTitle;
    this.parent = parent;
  }
  render() {}
}

export { Form };
